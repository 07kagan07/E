@model And.Eticaret.Core.Model1.Entity.Product
@{
    ViewBag.Title = "Detail";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link rel="stylesheet" href="~/Content/font-awesome.min.css" type="text/css">

<h2>Detail</h2>
<div>
    <div>@Model.Name</div>
    <div>@Model.Price</div>
</div>

@*<div class="form-group">
        @Html.LabelFor(model => model.CategoryID, "Kategori Adı", htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.DropDownList("CategoryID", null, htmlAttributes: new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.CategoryID, "", new { @class = "text-danger" })
        </div>
    </div>*@



<div>
    <a href="/" onclick="history.back();" data-pID="@Model.ID" class="addbasket btn btn-success">Sepete At</a>
</div>
<a href="/">Geri Dön</a>

@section script{
    <script type="text/javascript">
        $(function () {
            $(".addbasket").on("click", function () {
                var pid = $(this).attr("data-pID");
                $.post("/Basket/AddProduct?productID=" + pid + "&quantitiy=1", function (data) { });
                return
            });

        });
    </script>
}

